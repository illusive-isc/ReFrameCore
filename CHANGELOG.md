# Changelog — ReFrameCore

## 0.6.3

- Quest 変換が有効な設定を PC ターゲットのまま焼き込んだとき、テクスチャが PC 用 (DXT5) で書き出されていました。書き出した Texture2D はインポーターを持たないので、あとで Android に切り替えても圧縮し直されず、そのままでは Quest に上げられません。Quest 変換が有効なら Editor のターゲットに関わらず ASTC で書き出すようにし、記録のターゲットも「Quest」になります。

## 0.6.2

- 焼き込みで Quest 変換したテクスチャへの参照が切れ、Toon Lit のマテリアルが白くなっていたのを直しました。書き出しの順番 (参照される側のテクスチャ・メッシュ・クリップを先に) を揃え、移動後に全部を書き直すようにしました。
- 焼く前の姿を同じ置き場に `<名前> (焼き込み前).prefab` として控えるようにしました。ReFrame の設定がシーン上にしか無いアバターでも、焼き直したいときに戻せます。

## 0.6.1

- 焼き込みの記録 (`ReFrameBakedInfo`) を、アバターのルートではなく ReFrame のコンポーネントが付いていたオブジェクト (例: 子の `ReFrame`) に残すようにしました。
- 焼き込みとビルド時のスナップショットで、Unity の `AnimatorController.animationClips` を使わずステートを辿ってクリップを集めるようにしました。元から型の合わない遷移を持つコントローラー (例: `kaguya ear` の FaceLock) があると、呼ぶたびに Unity の警告が大量に出ていました。

## 0.6.0

- 「焼き込み」を足しました。ReFrame の Inspector 最下部のボタンで、ビルド時ではなく今その場で削除を適用し、FX / メニュー / パラメーター / 変更のあったクリップの実体を `Assets/ReFrameBaked/<元のプレハブ名>/<ヒエラルキー上の名前>/` に書き出してアバターを張り替え、同じ場所へ新しいプレハブとして保存します。ReFrame のコンポーネントは外れ、代わりに `ReFrameBakedInfo` (焼いた版・日時・固定したパラメーターの一覧の記録) が付きます。ビルド時に動くのは Optimizing の掃除 (Sweep) だけで、焼き付け前の状態は `ReFrameBakedInfo` が参照のまま持ち越します。掃除するかどうか (あらかじめ削除する / AAO にお任せする) は焼いた後も変えられます。中身が元と同じクリップは複製せず元のアセットを参照し続けます (名前や参照でクリップを探す他ツールのため)。kaguya で「元をビルド」と「焼いてからビルド」の結果 (レイヤー・パラメーター・メッシュ・Transform・コンポーネント) が一致することを確認しています。
- メニューの掃除・畳みを ReFrame が消した分に限りました。これまではメニュー全体を見て「空のサブメニュー」と「サブメニュー 1 個だけのサブメニュー」を宣言に関係なく片付けていたため、アバターのメニューに直接足したサブメニュー (後からギミックが中身を入れる空の置き場や、包みが 1 段だけのもの) が消えたり繋ぎ替わったりしていました。ReFrame の削除で空になったもの、削除で 1 個だけになったものだけを対象にし、Installer の設置先も「入るはずだった中身を ReFrame が全部消した」ときだけ掃除します。`subMenu` 未設定の項目も触りません。

## 0.5.2

- 行に付ける `[ReFrameDetachBlendShape]` を足しました。別の機能と同じクリップで動かされている BlendShape (例: アウター着脱と一緒に動く髪の `Outer_on`) を全クリップから外し、行を固定しても焼き付けません。値はメッシュの Inspector のスライダーだけで決まります。これまでは機能を消すと巻き添えでそのシェイプまで 0 に焼かれていました。

## 0.5.1

- アバターのルート `Animator` に Controller が刺さっていても、ビルドではそれを使わない扱いにしました (仮想化から外して実体からも外し、警告ログを出します)。これまでは NDMF がこの Controller をアバターのコントローラーとして書き戻すときに既定ステートが評価され、ReFrame が OFF に焼いた衣装などが元の既定値で ON に戻っていました (Animation ウィンドウで確認用に刺したまま上げると、消したはずの衣装が復活する)。

## 0.5.0

- 行に付ける `[ReFrameCollapseBlendTree]` を足しました。ポーズのように「他の条件で切り替わるステート」の BlendTree を値で固定するとき、選んだ枝のアニメーションをそのまま残します (これまでは焼き付けてアニメーションを消していたため、Humanoid のポーズは固定するとステートが空になっていました)。IKUSIA 系の立ち / しゃがみ / 伏せ / 浮遊ポーズの固定に適用しています。
- IKUSIA 系: ポーズを 0 以外で固定したとき、視点の入り直し (Temporary Pose Space) が毎フレーム走って視点が壊れていたのを直しました。ロコモーション削除時に何もしないまま残っていたレイヤーも消すようにしました。
- 行に付ける `[ReFrameNoBake]` を足しました。値を固定してアニメーションを消すとき、固定した枝のクリップをアバターへ焼き付けません (胸サイズのように、見た目は別の行で決めるもの向け)。
- `[ReFrameBlendShape]` を拡張しました。`"*"` で同名シェイプを持つ全メッシュを対象にでき、`SceneApply = false` で「ビルドでは固定するがシーンには書かない」を選べます。道連れ (BundleMember) の行でも固定が効きます。
- 行に付ける `[ReFrameRedirectState]` (ON 固定で別の枝へ常時入るための繋ぎ変え)、`[ReFrameValueLabels]` / `[ReFrameValueChoices]` (Value 欄の文言差し替え・候補プルダウン)、`[ReFrameRowOrder]` (見出し内の並び順)、`[ReFrameMenuFlatten]` (削除時にサブメニューの中身を親へ繰り上げ)、`[ReFrameBundleMember(Always = true)]` (代表が削除中なら値を問わず道連れ) を足しました。
- Inspector: ビット集計をパラメーター名で一意に数えるようにし、道連れの行を代表の直後へ並べ、「プレビューに反映」を ON/OFF ボタンにしました。アドオンが行タイトルや見出しの横に UI を差し込める拡張点 (行デコレーター) を足しました。
- プレビュー: BlendShape 行のスライダーを動かしたときにすぐ再計算するようにし、「アバターに反映」の行はプロキシを上書きしないようにしました。
- ビルドログに、固定で焼き付けた値の一覧をパラメーター別に出すようにしました (全文は `Temp/ReFrameBakeLog.txt`)。
- ビルド時のメニュー整理で、Modular Avatar の Menu Item 方式で入れるメニューの包みまで畳んでしまい項目に辿れなくなることがあったのを直しました。循環したメニューでビルドが止まらないようにもしました。

## 0.4.0

- 行に付ける `[ReFrameZeroChoice]` を足しました。メニューのどの項目も 0 を書かない数値の行 (尻尾の見た目のように、項目を全部 OFF にした 0 が既定の見た目になるもの) で、0 を「ギミック OFF」ではなく「選択なし」の正規の選択肢として扱います。Inspector の候補に「選択なし (0)」が常に出て、0 を選んでも「ギミック削除」表示になりません。
- ビルド時に複製したメニューを NDMF の ObjectRegistry に登録するようにしました。後から動くツール (TailEmbrace など) が、ReFrame の掃除済みメニューを元アセットの参照から辿れます。これまでは元アセットを直接参照している場所にだけ項目が入ることがありました。

## 0.3.0

- 行に付ける `[ReFrameCutByBlendShape]` を足しました。行を固定したとき (既定は ON 固定)、指定した BlendShape が動かす三角形をメッシュから切ります (服の下に隠れる体など)。`QuestOnly = true` で Quest 簡易対応のときだけ切れます。PC / Quest のプレビューにも出ます。

## 0.2.0

- Quest で透過マテリアルの枠をどう扱うかを選べるようにしました (`[ReFrameQuestTransparent]`)。「中身を焼き込んで不透明にする」は、袋の中身 (別パーツの不透明な面) を膜の向こう側から写してテクスチャに焼き込みます。「袋だけ消して中身を残す」は膜のパーツだけをメッシュから切ります。Inspector の Quest 節で切り替えられ、何も写らない所の色 (袋の色) もそこで選べます。
- 指定した BlendShape が動かす三角形を Quest で切る `[ReFrameQuestCutByBlendShape]` を足しました (頭の中から出す半透明パッチなど)。
- 焼き済みの置き場の「古い」判定が、プレビューの PC / Quest 切り替えやビルドターゲットで変わらなくなりました。焼き込みの調整値を変えたときは正しく古くなります。置き場の形式が変わったので、更新後に 1 回だけ焼き直しが要ります。
- Hierarchy の非表示プレビューで、消える塊の一番上の行 (プレハブの一番上など) は残して配下だけ隠すようにしました。親の子が全部消えるときは親の行を残します。

## 0.1.1

- ビルド後の掃除 (Sweep) が、ReFrame の固定・削除と関係ないものまで消していたのを直しました。消すのは「固定した結果、二度と有効にならなくなったもの」と、その巻き添えで使われなくなったボーンだけです。削除設定が 1 つも無いビルドでは、何も減りません。
- 他のツールが追加したオブジェクト (Bone Proxy で骨に付ける参照用オブジェクトなど) が消えたり、Hierarchy の非表示プレビューで隠れたりしなくなりました。
- 非表示プレビューは、固定で消えるものと `[ReFrameDeleteObject]` で名指ししたものだけを隠すようにしました。元から非アクティブなだけのものは隠しません。

## 0.1.0

- 更新のお知らせから、zip を直接ダウンロードして入れ替える「更新する…」を無くしました。更新は「更新ページを開く」で配布ページの unitypackage を取り込む方法だけにしています。
- 「VCC を開く」ボタンも外しました。

## 0.0.7

- Hierarchy の右クリックメニューを「ILLUSORY OVERRIDE → ReFrame」の下に移しました (Add Component と同じ場所)。
- README を利用者向けの説明に書き直しました。

## 0.0.6

- Inspector の一番上に、作者ショップ (BOOTH) の商品紹介を折りたたみで置きました。既定では閉じていて、ReFrame の設定には影響しません。開くと 3 件ずつ自動で切り替わり、◀ ▶ で送れます。

## 0.0.5

- 尻尾の向きのように「見た目の切り替え (variation / 本数) の下にある」ポーズを固定しても、切り替えごと消えてしまわないようにしました。固定した枝はシーンへ焼かず、そのポーズのクリップ (一致する枝が無いときはブレンドして作ったクリップ) に置き換えます。
- Merge Armature で本体へ統合するアクセサリのボーンが、固定後のポーズに付いてこなかったのを直しました (プレビュー・ビルドの両方)。
- 行の相互リンク `[ReFrameLinkedWith]` を足しました。片方を ON にするともう片方も ON になります (2D ブレンドの両軸など)。

## 0.0.4

- 更新のお知らせから「更新ページを開く」を押せるようにしました。開いたページのファイルを Unity に取り込むだけで、最新版に入れ替わります。
- VCC (または ALCOM) を開くボタンも足しました。
- パッケージの説明を分かりやすい文に書き直しました。

## 0.0.3

- プレビューの表示が重かったのを直しました。FX の解析結果をアバターごとに覚えておき、必要なときだけ作り直します。
- プレビューを NDMF の切り替えメニューから個別に止められるようにしました (項目名: ReFrame)。
- 配布先の一覧を独自ドメイン (https://reframe.illusive-isc.jp/vpm/index.json) に変更しました。

## 0.0.2

- 配布先の一覧を ReFrame の公開サイト (https://illusive-isc.github.io/ReFrame/vpm/index.json) に変更。Inspector の更新通知がこの一覧を見るようになります。

## 0.0.1

- 初版。
